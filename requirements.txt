Flask>=3.1.3
gunicorn>=26.0.0
watchdog>=6.0.0
apprise>=1.12.0

# kcc-c2e's CLI runtime deps. Baselines follow KCC's Docker image; KCC itself
# is installed with --no-deps because install_requires also pulls in PySide6,
# which is only needed for the GUI.
Pillow>=11.3.0
psutil>=7.2.2
requests>=2.34.2
python-slugify>=8.0.4,<9.0.0
packaging>=26.2
mozjpeg-lossless-optimization>=1.2.0
natsort>=8.4.0
numpy>=2.3.4
PyMuPDF>=1.26.6
